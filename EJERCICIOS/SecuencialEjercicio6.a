/* (Maquina expendedora de refrescos necesita un programa para estas maquinas
que realice el calculo de cuantas monedas de cada tipo debe devolver. Pra ello en
primer lugar, se introducira por teclado la cantidad a devolver en euros multiplo de centimos, 
que es la moneda mas pequeña de la que se dispone, es decir se tecleará 1.85 par 1 euro y 
85 centimos. Este programa escribirá en pantalla cuantas monedas de cada tipo hay que devolver 
teniendo en cuenta que:
Se dispone de monedas de 50 centimos,20, 10 y 5 centimos.
Siempre se dispone de las monedas necesarias de cada tipo, es decir, intentar devolver con las de mayor valor)*/

#include <stdio.h>   /*Que es la biblioteca standar de entrada y salida*/
                    /*printf , scanf, puts y gets*/

def devolvermonedas() {     /* Creamos la declaracion de la funcion DevolverMonedas*/

    cantidadcentimos = int (cantidadeuros*100);    /*declaramos variable, convertimos a cents*/
    int monedas[]=50,20,10,5;/*esto es un arreglo o vector []*/ /*para acceder a los elementos que contienen*/
    int cantidadmonedas=0                   

    for (int i =0; i <4; i++) {  /*Es un bucle, creando un contador i,para que se repita*/
                                /*Incicializando en 0, con la Condición i<4 elementos, e incrementandose en 1*/
        int cantidadmonedas = cantidadcentimos / monedas [i]; /*de las monedas del contador cuantas necesitamos*/
        /*si necesitamos monedas de este tipo la imprimimos, con el siguient..*/
    }
        if (cantidadmonedas > 0) { /*if es un bucle combinado con for */
            printf ("Monedas de %d centimos: %d\n", monedas[i], cantidadmonedas);
        }

            int main () {       /*declara funcion principal*/
            float cantidad; /*Inicializar la variable como flotante*/
             printf ("Introduce la cantidad a devolver en euros: ");
             scanf ("%f", &cantidad);

             devolvermonedas(cantidad); /*aqui llamamos a la funcion para que hago lo que sabe*/

            }
            return 0;
        
}


